# CI gate

The private MCP foundation is ready for automated install, typecheck, unit-test, production-build, and dependency-audit validation. Do not merge or deploy until the exact head commit passes those gates.
