# Platform claims policy

Only describe ChatGPT or Claude as supported after the corresponding real-client row in `CLIENT_VALIDATION.md` has passed against the deployed release SHA. Directory availability may be stated factually after approval, but must not be described as platform endorsement.
