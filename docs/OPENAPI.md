# Transport contract

The private AI surface is MCP-first. The browser delegation endpoints are narrow authenticated JSON APIs used only by Gapwise itself. The future public REST/OpenAPI surface for campus buildings, routing, and deterministic gap planning remains owned by the main Gapwise repository so this service does not duplicate domain truth.
