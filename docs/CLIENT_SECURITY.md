# Client-side safety expectations

Gapwise does not rely on a model provider's confirmation UI as its only write safeguard. Provider confirmation is welcome, but server-side delegation permissions, typed tool schemas, immutable academic meetings, revision checks, idempotency, and browser-side action application remain authoritative.
