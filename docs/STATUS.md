# Status

Implementation is in active private development. Do not publish the repository or list the connector publicly until authentication, RLS, encryption, revocation, cross-client tests, and write-safety tests pass against production-equivalent configuration.
