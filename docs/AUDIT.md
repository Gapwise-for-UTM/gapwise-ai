# Audit posture

Operational telemetry is metadata-only. The service should be auditable for who/when/which tool was called without retaining what the timetable contained. User-visible delegation status includes creation/update/revocation times and permissions, not model conversation content.
