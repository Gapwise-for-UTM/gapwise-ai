# Implementation gate

Before enabling the connector for real private data, complete the additive database migration, deploy the MCP runtime with a production-only encryption key, enable Supabase OAuth, and validate revocation plus cross-user isolation.
