# Provider neutrality

Provider-specific setup may define connector metadata, approval UX expectations, or documentation. It must not fork Gapwise's tool semantics, database schema, timetable interpretation, routing truth, or authorization model. ChatGPT and Claude are consumers of the same MCP server, not separate backends.
